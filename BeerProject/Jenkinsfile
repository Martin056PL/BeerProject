pipeline {
    agent any

    tools {
        maven "M3"
    }

    stages {
        stage('Compiling') {
            steps {
                dir('BeerProject') {
                    echo 'Compiling..'
                    sh 'mvn clean compile'
                }

            }
        }

//         stage('Test') {
//             steps {
//                 dir('BeerProject') {
//                     echo 'Testing..'
//                     sh 'mvn test'
//                 }
//             }
//         }

        stage('Sonarqube analyze') {
            steps {
                dir('BeerProject') {
                        echo JENKINS_TOKEN
                        echo 'run analyze..'
                        withSonarQubeEnv('SonarQube'){
                            sh 'mvn clean sonar:sonar -Dsonar.password=JENKINS_TOKEN'
                        }

                }
            }
        }

        stage("Quality gate") {
            steps {
                waitForQualityGate abortPipeline: true
            }
        }

        stage('Packaging') {
            steps {
                dir('BeerProject') {
                    echo 'Packaging....'
                    sh 'mvn package'
                }
            }
        }
    }
}